import setuptools

setuptools.setup(
    name="modules",
    version="0.0.1",
    description="Modules for GUAA",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)

setuptools.setup(
    name="GraphTransformerPyTorch",
    version="0.0.1",
    description="Modules for PyTorch Graph Transformers",
    packages=setuptools.find_packages(),
    python_requires='>=3.7',
)